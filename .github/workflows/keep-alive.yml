name: Keep Streamlit App Awake

on:
  schedule:
    - cron: "0 */4 * * *"
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest

    steps:
      - name: Visit Streamlit app
        run: |
          curl -I --max-time 30 https://kapabravo-calorie-calculator.streamlit.app
